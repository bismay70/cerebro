# Example only — copy real values into .env.local (gitignored).
# This site is static and has no backend of its own — the only env var it
# reads is the canonical URL used for metadata (og:image base, etc).
NEXT_PUBLIC_SITE_URL=http://localhost:3001
